language: node_js
node_js:
    - '20'

services:
    - docker

cache:
    directories:
        - '$HOME/.npm'

install:
    - npm ci

script:
    - npm run lint
    - npm run build
    - docker build -t taskcontrol .
